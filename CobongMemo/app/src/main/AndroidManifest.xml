<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cobong.jeongwoojin.cobongmemo.cobongmemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name=".view.memo.MemoApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Splash Activity -->
        <activity
            android:name=".StartActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity android:name=".view.memo.textmemo.TextMemoWriteActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".view.memo.textmemo.TextMemoView" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".view.memo.handwritememo.HandwritingActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".view.memo.handwritememo.HandwriteViewActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>